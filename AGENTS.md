# כללי עבודה — mondial2026-family-pool

מסמך עבודה מחייב. כל פעולה בפרויקט הזה כפופה לכללים הבאים.

## בידוד הקשר (מחייב)
- **אין** שימוש ב-Astro_X / V13 — לא קוד, לא tests, לא docs, לא conventions, לא זיכרון.
- **אין** שימוש ב-Claude Preview אם הוא מחובר ל-V13 (הוא מצא servers של Astro_X — נפסל).
- **אין** קריאת קבצים מחוץ ל-`/Users/admin/projects/mondial2026-family-pool`.
- **אין** reuse של קוד או מסמכים מפרויקט אחר. כל החלטה נגזרת ממסמכי הפרויקט הזה בלבד.
- לפני כל משימה: לאמת שה-`pwd` מתחיל ב-`/Users/admin/projects/mondial2026-family-pool`; אחרת לעצור.

## ארכיטקטורה נוכחית
Firebase Hosting (קבצים סטטיים ב-`public/`). צד-לקוח בלבד: HTML + CSS + JS.
**אין** כרגע: Firestore, Auth, Functions, Analytics, מודל נתונים בשרת.
אחסון מקומי: `localStorage` (זיהוי + ניחושים). מקור משחקים: `public/matches.json`.

## כלל מוצר — התנהגות ניחושים
- המשתתף יכול למלא ניחושים לכל המשחקים **מראש**.
- כל משחק ניתן לעריכה **עד זמן הפתיחה שלו** (`kickoff`).
- אחרי `kickoff` המשחק **נעול** (קריאה בלבד).
- משחק שלא מולא = אין ניחוש (יקבל 0 בהמשך, כשיהיה ניקוד).
- **אין submit סופי לכל הטורניר** — שמירה היא אוטומטית, פר-משחק.

(הנעילה עצמה טרם מומשה; המודל וה-UI מעוצבים כך שלא יסתרו אותה.)
